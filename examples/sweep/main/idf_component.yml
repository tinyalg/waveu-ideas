version: "0.1.0"
description: Sweep Example

dependencies:

# The component name without namespace must match the name of the top level directory.
  tinyalg/waveu:
    git: https://github.com/tinyalg/waveu.git
